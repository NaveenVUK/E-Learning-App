
const userInitialState = {}

const UserRedcuers = (state = userInitialState, Action) => {
    switch (Action.type) {

        default: {
            return state
        }
    }
}

export default UserRedcuers